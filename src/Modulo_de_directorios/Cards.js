/* Definimos la classe de naipes, donde cada objecto representa una carta*/

class Card {
    constructor (card_type,card_text, card_value) {
        this.type = card_type;
        this.text = card_text;
        this.value = card_value;
    }
}
